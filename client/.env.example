# Where the Vite dev proxy forwards to (see vite.config.ts). Origin only — no path.
# Must match the port the server actually listens on: server/.env's PORT.
# The socket entry's ws:// target is derived from this in the config, so there is one value here,
# not two.
SERVER_ORIGIN=
